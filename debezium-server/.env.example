# ============================================
# Azure Event Hubs Configuration
# ============================================
# IMPORTANT: Update these values with your actual Azure credentials

# Event Hub Namespace (without .servicebus.windows.net)
AZURE_EVENTHUB_NAMESPACE=your-eventhub-namespace

# Shared Access Key Authentication
SAS_KEY_NAME=RootManageSharedAccessKey
SAS_KEY=your-shared-access-key-here

# Event Hub Names (Topics)
ORACLE_EVENTHUB_NAME=oracle-cdc
POSTGRES_EVENTHUB_NAME=postgres-cdc

# ============================================
# Full Connection Strings (Optional)
# ============================================
# If you prefer to use full connection strings instead of components above
# Uncomment and use these:
# ORACLE_EVENTHUBS_CONNECTION_STRING=Endpoint=sb://your-namespace.servicebus.windows.net/;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=your-key;EntityPath=oracle-cdc
# POSTGRES_EVENTHUBS_CONNECTION_STRING=Endpoint=sb://your-namespace.servicebus.windows.net/;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=your-key;EntityPath=postgres-cdc

# ============================================
# Data Format Configuration
# ============================================
# Options: json, avro
# Change this to switch between JSON and Avro format
DATA_FORMAT=json
# DATA_FORMAT=avro

# Enable JSON schemas (true/false)
# When true, includes schema information in JSON messages
# JSON_SCHEMAS_ENABLE=false
JSON_SCHEMAS_ENABLE=true
